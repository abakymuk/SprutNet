# T14. Нормализация времени и таймзон

**Goal:** Единая модель времени: хранить UTC, показывать Local (POL/POD), избегать путаницы.

**Scope:**
- In: утилиты для конвертации UTC→Local с учётом таймзоны порта, двойной рендер (Local + мелким шрифтом UTC).
- Out: консистентный показ времени в карточках и дедлайнах.

**AC (Gherkin):**
- Given дедлайн с UTC, When рендерю Local по таймзоне POL, Then пользователь видит локальное время и UTC-хинт.
- Given API отдаёт `localTime` и `timeZone`, Then используем их как источник истины.

**DoD:**
- [x] Утилиты времени покрыты unit-тестами (краевые случаи DST).
- [x] UI показывает Local и UTC единообразно.
- [x] Документация: правила отображения времени.

## Реализация завершена ✅

### Что было реализовано:

#### 1. Утилиты времени (`apps/web/src/lib/utils/timezone.ts`)
- ✅ `convertUTCToLocal()` - конвертация UTC→Local
- ✅ `formatLocalTime()` - форматирование локального времени
- ✅ `getTimezoneOffset()` - получение смещения таймзоны
- ✅ `isDST()` - определение летнего времени
- ✅ `handleDSTTransitions()` - обработка переходов DST
- ✅ `getTimezoneAbbreviation()` - сокращения таймзон
- ✅ `formatTimeForDisplay()` - форматирование для UI
- ✅ `isValidTimezone()` - валидация таймзон
- ✅ `getPopularTimezones()` - популярные таймзоны

#### 2. Компоненты UI
- ✅ `TimeDisplay` - основной компонент отображения времени
- ✅ `TimeDisplayCompact` - компактная версия
- ✅ `TimezoneInfo` - информация о таймзоне
- ✅ `TimezoneSelector` - выбор таймзоны

#### 3. Обновленные типы
- ✅ `PortRef.timezone` - таймзона порта
- ✅ `Sailing.originTimezone/destinationTimezone` - таймзоны портов
- ✅ `Sailing.departureLocalTime/arrivalLocalTime` - локальное время
- ✅ `Deadline.timezone` - таймзона дедлайна

#### 4. Интеграция в существующие компоненты
- ✅ `DeadlinesModal` - использование TimeDisplay для дедлайнов
- ✅ `SailingResults` - отображение ETD/ETA с таймзонами
- ✅ Добавлена информация о таймзоне в Route Info

#### 5. Unit-тесты (`apps/web/src/lib/__tests__/timezone.test.ts`)
- ✅ Тесты всех утилит времени
- ✅ Краевые случаи DST (весенний/осенний перевод)
- ✅ Обработка неизвестных таймзон
- ✅ Международные таймзоны
- ✅ Несуществующее и дублированное время

#### 6. Документация
- ✅ `docs/TIMEZONE_GUIDE.md` - полное руководство по работе с таймзонами
- ✅ Правила отображения времени
- ✅ Примеры использования
- ✅ Рекомендации по миграции

### Результат:
**Единая модель времени реализована!** Теперь приложение:
- Хранит все время в UTC
- Показывает локальное время портов
- Отображает UTC как подсказку
- Корректно обрабатывает DST переходы
- Имеет полное покрытие тестами
- Содержит подробную документацию
